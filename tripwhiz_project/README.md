TripWhiz - Full Project (Frontend + Backend)

Run backend:
cd backend
npm install
cp .env.example .env  # set JWT_SECRET
npm run dev

Run frontend:
cd frontend
npm install
npm run dev

Notes:
- Backend serves uploads at /uploads
- Frontend uses Leaflet for maps and Tailwind for styling
